version: 2
updates:

  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    # Don't raise PRs for releases younger than this many days — dodges a
    # freshly-poisoned version that gets yanked within hours/days. 1–90; tune to taste.
    cooldown:
      default-days: 7

  # Maintain dependencies for Composer
  - package-ecosystem: "composer"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 7

  # Maintain npm dependencies (Vite/Tailwind frontend)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 7

  # Keep base-image pins fresh (Dockerfile, docker-compose.yml)
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 7
